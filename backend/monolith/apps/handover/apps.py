from django.apps import AppConfig


class HandoverConfig(AppConfig):
    default_auto_field = "django.db.models.BigAutoField"
    name = "apps.handover"
    label = "handover"
    verbose_name = "V1 Deal handover"
