# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.iml

# Build artifacts (kept local, not committed)
build/
**/build/
.dart_tool/
.gradle/

# Env / secrets
.env
.env.*
!.env.example
*.pem
*.key

# Logs
*.log

# AI
.claude
.agentbridge/
docs/claude-sessions/*.jsonl
graphify-out/
**/graphify-out/

# Generated Python/tool state
__pycache__/
**/__pycache__/

# Local screenshot helper copied into the mobile font asset tree.
/mobile/assets/fonts/tools/
/mobile/l10n_untranslated.json

# Local toolchain downloads (Flutter SDK fetched for verification runs)
.codex-temp/

# Local-only test settings modules. `test_local.py` runs the suite on in-memory
# SQLite; `test_pg.py` boots an embedded PostgreSQL (the `pgserver` package, a
# developer aid that is deliberately not in requirements.txt) so the
# PostgreSQL-only gates — SKIP LOCKED, partial unique indexes, concurrency —
# can run on a workstation without Docker. CI uses a real server.
backend/monolith/config/settings/test_pg.py
build/phase8fa-apk/
